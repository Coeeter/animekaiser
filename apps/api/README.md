# AnimeKaiser API

The AnimeKaiser backend application. It composes HTTP routes, authentication, database and Redis adapters, RPC handlers, email delivery, and background workers.

## Development

```bash
bun run dev
```

Build and run the production server with `bun run build` and `bun run start`. Configure required services and credentials using the repository `.env.example` file.
