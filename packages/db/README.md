# Database

PostgreSQL access and Drizzle schema for authentication, profiles, libraries, external accounts, watch history, streaming mappings, and jobs.

## Commands

```bash
bun run dev
bun run db:generate
bun run db:migrate
bun run db:studio
```
