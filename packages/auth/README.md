# Auth

Better Auth configuration and typed server and web helpers. Authentication tables are defined in `@animekaiser/db`.

The package also provides the `auth:generate` command for regenerating the Better Auth Drizzle schema.
